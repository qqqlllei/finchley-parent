server:
  port: 8081
info:
  version: @version@
  isGray: true
spring:
  cloud:
    nacos:
      discovery:
        metadata: {"version":@version@}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
  server:
    servlet:
      context-path:

