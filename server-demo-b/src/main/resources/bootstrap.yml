spring:
  application:
    name: demo-b
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        metadata: {"serverList":["1","2","3"]}
        ip: 10.33.50.40
      config:
        server-addr: localhost:8848
        file-extension: yml

    inetutils:
      preferred-networks: 10.0

